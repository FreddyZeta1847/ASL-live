# Current task

The active task in this session. Update on every session start and
whenever scope shifts. Keep it short — this file is for orientation, not
documentation.

---

## Now

**Phase 1 — data collection pipeline.**

Plan: [`phases/phase-1-data-collection.md`](phases/phase-1-data-collection.md).

Status: plan approved, no code written yet. The 5 commits in the phase
plan are queued.

Next concrete step:
1. Bootstrap project skeleton (commit 1) — `pyproject.toml`,
   `.gitignore`, package `__init__.py` files, `src/asl_live/config.py`.

## Recently shipped

- 2026-04-29 — Restructured `.claude/` into `docs/` + `plans/`
  layout per user request. `discussion.md` → `architecture.md` +
  `tech-stack.md`; `sub_features.md` → 10 files in `docs/features/`;
  `phase_1.md` moved to `plans/phases/phase-1-data-collection.md`.
- 2026-04-29 — Phase 1 implementation plan written and approved.
- All 10 sub-features locked with full rationale (in `docs/features/`).
- Top-level design locked (in `docs/architecture.md` + `tech-stack.md`).
- `PLAN.md` is the master roadmap of phases.

## Blocked / waiting on

(nothing)
